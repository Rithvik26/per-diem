{
  "buildCommand": "cd ../.. && npm install && npm run build -w packages/shared-types && npm run build -w apps/frontend",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping install, handled in buildCommand'",
  "framework": null,
  "devCommand": "npm run dev"
}
